#include <stdio.h>
#include <stdlib.h>

int main()
{
    char A[] = "Welcome";
    int i;
    for (i = 0; A[i] != '\0'; i++)
    {
    }
    printf("The length of the string is: %d", i);

    return 0;
}